# Copy one of these to start:
#   cp .env.local.example .env.local  → npm run dev:local (laptop Supabase)
#   cp .env.dev.example   .env.dev    → npm run dev:dev   (cloud dev project)
#   cp .env.prod.example  .env.prod   → npm run dev:prod  (production — careful!)
